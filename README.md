# NYC-Taxi-Trip-Duration-Prediction

Regression project to predict **NYC taxi trip duration** (in seconds) from pickup/dropoff coordinates, time, and related features. The model is served via a small FastAPI app.

## Project structure

```
trip duration/
├── README.md
├── requirements.txt
├── .gitignore
├── data/                 # Train, validation, test (and cleaned) CSVs
│   ├── train.csv
│   ├── val.csv
│   ├── test.csv
│   └── cleaned_data.csv
├── src/                  # Core logic
│   ├── __init__.py
│   ├── preprocessing.py  # TripDataPreprocessor (features, haversine, time)
│   └── evaluate.py       # Evaluation helpers (R², MSE)
├── notebooks/            # EDA and model training
│   ├── 01_eda.ipynb      # Exploratory data analysis
│   └── 02_model.ipynb    # Train Ridge pipeline, save model
├── api/                  # Prediction API
│   ├── __init__.py
│   └── main.py           # FastAPI app, /predict endpoint
├── models/               # Saved model artifact
│   └── model.pkl         # (generated by 02_model.ipynb)
└── scripts/
    └── run_evaluate.py   # Evaluate on val + test sets
```

## Setup

From the project root (`trip duration/`):

```bash
python -m venv .venv
source .venv/bin/activate   # or .venv\Scripts\activate on Windows
pip install -r requirements.txt
```

Place (or generate) `train.csv`, `val.csv`, `test.csv`, and optionally `cleaned_data.csv` in `data/`.  
If you still have the original `Data/` folder, you can copy from it:

```bash
cp Data/*.csv data/
```

## Training

1. Run **`notebooks/01_eda.ipynb`** to explore the data (uses `data/train.csv`).
2. Run **`notebooks/02_model.ipynb`** to train the Ridge pipeline and save **`models/model.pkl`** (reads `data/cleaned_data.csv`).

Run notebooks from the project root so that `../data/` and `../models/` resolve correctly, or set the notebook kernel cwd to the project root.

## Evaluation

With `models/model.pkl` and `data/val.csv`, `data/test.csv` in place:

```bash
python scripts/run_evaluate.py
```

This uses `src.evaluate.load_and_evaluate` to preprocess, load the model, and print R² and MSE for validation and test sets.

## API

Start the prediction API (loads `models/model.pkl` or, if missing, `model.pkl` in the project root):

```bash
uvicorn api.main:app --reload --host 127.0.0.1 --port 8000
```

- **Docs:** http://127.0.0.1:8000/docs  
- **POST /predict:** send a JSON body with trip fields; response includes `predicted_duration` (minutes).

Example request body:

```json
{
  "id": "id123",
  "vendor_id": 2,
  "pickup_datetime": "2016-06-08 07:36:19",
  "pickup_longitude": -73.9856,
  "pickup_latitude": 40.7359,
  "dropoff_longitude": -73.9803,
  "dropoff_latitude": 40.7605,
  "passenger_count": 1,
  "store_and_fwd_flag": "N"
}
```

## Data

Input columns: `id`, `vendor_id`, `pickup_datetime`, `passenger_count`, `pickup_longitude`, `pickup_latitude`, `dropoff_longitude`, `dropoff_latitude`, `store_and_fwd_flag`.  
Target: `trip_duration` (seconds).  
Preprocessing (in `src.preprocessing.TripDataPreprocessor`): time features (hour, day of week, month), haversine distance, log1p transforms, and dropping unused columns.

## License

Use and adapt as needed for your environment.
